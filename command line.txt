#initialize the npm and creates the package.json
npm init

#--save adds this dependancies to tha package.json
npm install express body-parser ejs --save

npm install from slients folder

#to live updating of the server
npm install -g nodemon 

#to run the server
nodemon

front end style manage
npm install -g bower

bower install bootstrap